# MotoSim 中 MOVL 路径碰撞栏杆问题分析

> 现象：规划器里每一步都做了碰撞检测、插值步长以为是 1mm，但导出到 MotoSim 后，
> 在 PLAY 模式下两个 MOVL 之间的运动仍然撞到栏杆。

---

## 一、结论先行

**在规划器里逐点校验过的那条轨迹，并不是 MotoSim 实际执行的那条轨迹。**

- 你校验的是「规划器自己选的 IK 构型」在离散点上的静态碰撞；
- MotoSim 执行 MOVL 时，只拿到两个端点的关节脉冲，**中间过程由控制器自己重新插值、自己重新选构型**；
- 两者中间构型不一致 → 手臂（肘部 / link2 / link4）扫过的空间不同 → 撞栏杆。

端点姿态被脉冲锁死，但**中间过程的构型不归你管**。同一个 TCP 位姿，肘上/肘下、腕翻转是不同手臂姿态，一个能躲开栏杆、一个会撞上。你校验的是前者，MotoSim 走的是后者。

---

## 二、证据链（代码定位）

### 1. 校验步长其实是 5mm，不是 1mm

| 位置 | 内容 |
|------|------|
| `RobotConfig.h:65` | `double minInterpolateDist = 5.0;` —— 默认 5mm |
| `two_orientation_interpolation.cpp:126` | `stepMm = config.minInterpolateDist > 0 ? config.minInterpolateDist : 1.0;` |
| `main.cpp` (cfg 构建处 430~456 行) | **从未给 `cfg.minInterpolateDist` 赋值** → 一直用默认 5mm |

所谓「1mm 步长」在这条代码路径上没有生效，实际是每 5mm 采一个校验点。

### 2. 校验过的中间插值点，导出前被全部丢掉（关键）

| 位置 | 内容 |
|------|------|
| `ViewpointPlanner.cpp:301-311` (`linePathPredict`) | 直线上「直接可达」的中间点被标成 `isImportant=false, isPathJoint=false` |
| `process_line_result.cpp:420-428` | `keepOnlyKeyNodesAtEnd` 只保留 `isImportant \|\| isPathJoint` 的关键点，**把所有普通可达中间点删除** |

结果：一条长直线段最终只剩两个端点 —— 即截图里看到的「两个 MOVL」。

### 3. 导出的是关节脉冲端点，中间由 MotoSim 重新插值

| 位置 | 内容 |
|------|------|
| `JBIExporter.cpp:136` | `///POSTYPE PULSE`，每个点写的是 `C00000=脉冲...`（关节脉冲） |
| MOVL 语义 | TCP 走笛卡尔直线；MotoSim 在两脉冲端点间**自己重新算直线、自己逐点解 IK 选构型** |

---

## 三、根因串联

```
规划器用「它自己选的 IK 构型」在 5mm 离散点上验证直线 → 通过
        ↓
只导出两个端点（中间点被 keepOnlyKeyNodesAtEnd 删掉）
        ↓
MotoSim 执行 MOVL，用「它自己选的构型」重新插值这条直线
        ↓
中间肘部 / 手腕构型与校验时不同 → 扫掠空间不同 → 撞栏杆
```

### 另需单独确认的两点

1. **扫掠体未校验**：`RobotReachability.cpp:272` 的 `checkCollision(robot, jv, jv)`
   只检测单个离散静态构型，**不检测两个构型之间的扫掠体**。
2. **栏杆是否在规划器的碰撞世界里**：MotoSim 单元里有栏杆，不代表
   `KinematicsApi` / `SceneDocument` 的碰撞模型注册了它。若规划器碰撞世界
   里没有这根栏杆，**再细的步长也查不出来**。

---

## 四、修复建议（按性价比排序）

1. **让导出轨迹 = 校验过的轨迹（根治）** —— 不要只导出两端点，二选一：
   - 关闭 / 不启用 `keepOnlyKeyNodesAtEnd`，或把直线段上校验通过的中间插值点
     也标成关键点，**带关节脉冲一起导出成多点 MOVL**；
   - 或把长 MOVL 显式拆成若干短 MOVL，每段中点都是已验证的脉冲点。

2. **真正把步长设成 1mm**：在 `main.cpp` 构建 cfg 处加
   `cfg.minInterpolateDist = 1.0;`，确认 5mm → 1mm。

3. **确认栏杆在规划器碰撞模型里**：检查栏杆有没有作为碰撞体加入
   `SceneDocument` / `KinematicsApi` 的碰撞集合。

4. **保证构型连续**：导出端点时确认整条 MOVL 上 IK 不发生分支跳变 / 腕部翻转
   （form 一致）；不一致就必须用第 1 条的多点导出。

5. **核对姿态插值方式**：`twoOrientationInterpolation`（slerp 等）与 MotoSim
   MOVL 的姿态插值是否一致，否则即使带中间点，姿态轨迹也会有偏差。

---

## 五、最直接的验证实验

把那条撞栏杆的 MOVL 段，**带上中间插值点的关节脉冲全部导出**，再在 MotoSim 里 PLAY：

- **不撞了** → 根因是「丢中间点 + 控制器自选构型」（第二、三节）；
- **还撞** → 根因是「栏杆压根没进规划器的碰撞模型」（第三节第 2 点）。

---

## 六、涉及文件清单

| 文件 | 作用 |
|------|------|
| `RobotConfig.h` | `minInterpolateDist` 默认值定义 |
| `two_orientation_interpolation.cpp` | TCP 空间姿态插值，步长取自 cfg |
| `ViewpointPlanner.cpp` | `linePathPredict` / `measLinePathPredict` 逐点 IK + 碰撞校验 |
| `RobotReachability.cpp` | `checkCollision` 静态构型碰撞检测 |
| `process_line_result.cpp` | `keepOnlyKeyNodesAtEnd` 过滤中间点 |
| `JBIExporter.cpp` | 导出 `POSTYPE PULSE` 关节脉冲端点 + MOVL/MOVJ 指令 |
| `main.cpp` | 构建 cfg、组织三阶段规划主流程 |
